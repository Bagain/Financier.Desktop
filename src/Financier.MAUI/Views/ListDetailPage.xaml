<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	x:Class="Financier.MAUI.Views.ListDetailPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:m="clr-namespace:Financier.MAUI.Models"
	xmlns:vm="clr-namespace:Financier.MAUI.ViewModels"
	x:DataType="vm:ListDetailViewModel">
	<RefreshView Command="{Binding RefreshingCommand}" IsRefreshing="{Binding IsRefreshing}">
		<CollectionView
			ItemsSource="{Binding Items}"
			RemainingItemsThreshold="10"
			RemainingItemsThresholdReachedCommand="{Binding LoadMoreCommand}">
			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="m:SampleItem">
					<Border
						Margin="4"
						Padding="12"
						BackgroundColor="{AppThemeBinding Light={StaticResource White},
						                                  Dark={StaticResource Black}}"
						SemanticProperties.Description="{Binding Title}"
						StrokeShape="RoundRectangle 8">
						<Border.GestureRecognizers>
							<TapGestureRecognizer
								Command="{Binding x:DataType=vm:ListDetailViewModel, Source={RelativeSource AncestorType={x:Type vm:ListDetailViewModel}}, Path=GoToDetailsCommand}"
								CommandParameter="{Binding Source={RelativeSource AncestorType={x:Type m:SampleItem}}, Path=.}" />
						</Border.GestureRecognizers>
						<Label
							FontSize="Large"
							Text="{Binding Title}"
							TextColor="{AppThemeBinding Light={StaticResource Primary},
							                            Dark={StaticResource Gray100}}" />
					</Border>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
	</RefreshView>
</ContentPage>
